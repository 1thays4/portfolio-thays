{
  "private": true,
  "scripts": {
    "deploy": "npx wrangler pages deploy dist --branch main",
    "preview": "npx wrangler pages dev dist",
    "build": "node build.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
